<?xml version="1.0" encoding="utf-8" ?>
<views:ContentPageBase xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:views="clr-namespace:Baustellen.App.Client.Views"
             x:Class="Baustellen.App.Client.Views.MainPage"
             xmlns:viewmodels="clr-namespace:Baustellen.App.Client.ViewModels"
             x:DataType="viewmodels:MainPageViewModel">

    <views:ContentPageBase.ToolbarItems>
        <ToolbarItem Text="Add" Command="{Binding AddProjectCommand}" IconImageSource="{StaticResource AddIconImageSource}" />
    </views:ContentPageBase.ToolbarItems>

    <VerticalStackLayout>
        <ScrollView>
            <VerticalStackLayout
                Padding="30,0"
                Spacing="25">

                <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}"></ActivityIndicator>

                <Image
                    Source="dotnet_bot.png"
                    HeightRequest="185"
                    Aspect="AspectFit"
                    SemanticProperties.Description="dot net bot in a race car number eight" />

                <Label
                    Text="Hello, World!"
                    Style="{StaticResource Headline}"
                    SemanticProperties.HeadingLevel="Level1" />

                <Label
                    Text="Welcome to &#10;.NET Multi-platform App UI"
                    Style="{StaticResource SubHeadline}"
                    SemanticProperties.HeadingLevel="Level2"
                    SemanticProperties.Description="Welcome to dot net Multi platform App U I" />

            </VerticalStackLayout>
        </ScrollView>
    </VerticalStackLayout>
</views:ContentPageBase>
